{
  "name": "websocket_server",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "db:generate": "bunx prisma generate --schema=../prisma/schema.prisma"
  },
  "dependencies": {
    "prisma": "^6.6.0",
    "@prisma/client": "^6.6.0",
    "@prisma/adapter-mariadb": "^6.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}